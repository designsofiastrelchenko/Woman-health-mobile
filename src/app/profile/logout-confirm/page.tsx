import { ProfileAccountDangerModalView } from "@/components/figma-screens/profile-account-danger-modal-view";

/** Figma 92:25269 — Profile Page / Log out. */
export default function ProfileLogoutConfirmPage() {
  return <ProfileAccountDangerModalView variant="logout" />;
}
