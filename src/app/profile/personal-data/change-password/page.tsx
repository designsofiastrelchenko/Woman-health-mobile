import { ProfileChangePasswordView } from "@/components/figma-screens/profile-change-password-view";

/** Figma 92:27232 — Change password. */
export default function ProfileChangePasswordPage() {
  return <ProfileChangePasswordView />;
}
