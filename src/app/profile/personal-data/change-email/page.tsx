import { ProfileChangeEmailView } from "@/components/figma-screens/profile-change-email-view";

/** Figma 192:3929 — Change email. */
export default function ProfileChangeEmailPage() {
  return <ProfileChangeEmailView />;
}
