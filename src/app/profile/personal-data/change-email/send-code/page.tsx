import { ProfileChangeEmailSendCodeView } from "@/components/figma-screens/profile-change-email-send-code-view";

/** Figma 192:4013 — Send code. */
export default function ProfileChangeEmailSendCodePage() {
  return <ProfileChangeEmailSendCodeView />;
}
