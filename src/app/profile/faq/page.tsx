import { ProfileFaqView } from "@/components/figma-screens/profile-faq-view";

/** Figma 92:26598 — Profile Page / FAQ. */
export default function ProfileFaqPage() {
  return <ProfileFaqView expandedFirst={false} />;
}
