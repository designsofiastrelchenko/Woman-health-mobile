import { ProfileFigmaView } from "@/components/figma-screens/profile-figma-view";

/** Figma 91:20934 — Profile Page / Profile. */
export default function ProfilePage() {
  return <ProfileFigmaView />;
}
