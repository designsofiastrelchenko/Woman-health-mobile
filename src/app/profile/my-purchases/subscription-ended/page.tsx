import { ProfileMyPurchasesEndedView } from "@/components/figma-screens/profile-my-purchases-ended-view";

/** Figma 192:4414 — My purchases / Subscription ended. */
export default function ProfileMyPurchasesSubscriptionEndedPage() {
  return <ProfileMyPurchasesEndedView />;
}
